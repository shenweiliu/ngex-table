
export enum PaginationType {
    server,
    client
}

export enum TableChange {
    init,
    search,
    pageNumber,
    pageSize,
    sorting
}

export enum SortChangedType {
    init,
    column,
    cancel,
    clearAll,
    option
}

export enum SortingTypeLocation {
    commandPanel,
    optionPanel    
}

